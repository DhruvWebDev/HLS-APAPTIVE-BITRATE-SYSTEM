import { expect, test } from "bun:test";

test("Uploading single video to the s3 bucket should work", () => {
  expect(true).toBe(true);
});

test("Uploading multiple videos to the s3 bucket should work", () => {
  expect(true).toBe(true);
});

test("Uploading single incomplete video file payload to the backend to upload to the  s3 bucket shouldn't work", () => {
  expect(true).toBe(true);
});

test("Uploading multiple incomplete video file payload to the backend to upload to the  s3 bucket shouldn't work", () => {
  expect(true).toBe(true);
});


test("Poll the backend with id of the request to know about the status of the uploading file to s3", () => {
  expect(true).toBe(true);
})

